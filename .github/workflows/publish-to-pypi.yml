name: Publish PyGeoweaver Python 🐍 distributions 📦 to PyPI and TestPyPI

on:
  release:
    types: [published]  # Trigger the workflow when a release is published

jobs:
  test-installation:
    name: Test Python Installation for 3.8, 3.9, 3.10, 3.11
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.8', '3.9', '3.10', '3.11']
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      # Geoweaver is Spring Boot 2.4 / Java 11; ubuntu-latest default JDK is often too new.
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'
      - name: Install Package in Editable Mode
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
      - name: Test PyGeoweaver
        run: |
          java -version
          gw start
          exit_code=$?
          if [ $exit_code -eq 0 ]; then
            echo "PyGeoweaver started successfully"
          else
            echo "Error: PyGeoweaver failed to start"
            echo "---- ~/geoweaver.log ----"
            tail -n 120 ~/geoweaver.log || true
            exit $exit_code
          fi

  build-n-publish:
    needs: test-installation
    name: Build and publish Python 🐍 distributions 📦 to PyPI and TestPyPI
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.x"
    - name: Install pypa/build
      run: >-
        python -m
        pip install
        build
        --user
    - name: Build a binary wheel and a source tarball
      run: >-
        python -m
        build
        --sdist
        --wheel
        --outdir dist/
    
    - name: Check GitHub ref
      run: echo ${{ github.ref }}
    
    - name: Publish distribution 📦 to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        password: ${{ secrets.PYPI_API_TOKEN }}
